# This is a Declarative Automation Bundle definition for NeoBankBundle.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: NeoBankBundle
  uuid: 866e6ce8-1987-4f23-a37f-928e8695ffee

include:
  - resources/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-434ee770-dc97.cloud.databricks.com
    variables:
      catalog: dev_banking
      schema: dev
  prod:
    mode: production
    workspace:
      host: https://dbc-434ee770-dc97.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/kdeepika11@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/kdeepika11@gmail.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: prod_banking
      schema: prod
    permissions:
      - user_name: kdeepika11@gmail.com
        level: CAN_MANAGE
